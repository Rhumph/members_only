<nav>
    <ul>
      <% links.forEach(function(link) { %>
        <li>
          <a href="<%= link.href %>">
            <span> <%= link.text %> </span>
          </a>
        </li>
      <% }); %>
    </ul>
  </nav>
  